<icecast>
    <location>Earth</location>
    <admin>icemaster@localhost</admin>

    <limits>
        <clients>200</clients>
        <sources>32</sources>
        <queue-size>524288</queue-size>
        <client-timeout>30</client-timeout>
        <header-timeout>15</header-timeout>
        <source-timeout>10</source-timeout>
        <burst-on-connect>1</burst-on-connect>
        <!-- ~16s of 128k MP3 so IMVU listeners catch near track start after cold-restart encode -->
        <burst-size>262144</burst-size>
    </limits>

    <authentication>
        <!-- Must match ICECAST_SOURCE_PASSWORD in .env / Laravel (default: lurkbot_dev) -->
        <source-password>lurkbot_dev</source-password>
        <relay-password>lurkbot_dev</relay-password>
        <admin-user>admin</admin-user>
        <admin-password>hackme</admin-password>
    </authentication>

    <hostname>localhost</hostname>

    <listen-socket>
        <port>8001</port>
        <bind-address>127.0.0.1</bind-address>
    </listen-socket>

    <http-headers>
        <header name="Access-Control-Allow-Origin" value="*" />
    </http-headers>

    <fileserve>1</fileserve>

    <paths>
        <basedir>/opt/homebrew/opt/icecast/share/icecast</basedir>
        <logdir>infra/icecast/logs</logdir>
        <webroot>/opt/homebrew/opt/icecast/share/icecast/web</webroot>
        <adminroot>/opt/homebrew/opt/icecast/share/icecast/admin</adminroot>
        <alias source="/" destination="/status.xsl"/>
    </paths>

    <logging>
        <accesslog>access.log</accesslog>
        <errorlog>error.log</errorlog>
        <loglevel>3</loglevel>
        <logsize>10000</logsize>
    </logging>

    <security>
        <chroot>0</chroot>
    </security>
</icecast>
