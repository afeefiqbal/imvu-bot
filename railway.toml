[build]
buildCommand = "composer install --no-dev --optimize-autoloader && npm install && npm run build"

[deploy]
startCommand = "bash scripts/railway-web-and-bot.sh"
preDeploy = "php artisan migrate --force"
